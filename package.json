{
  "name": "status-list-mock",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "sam build --template template.yaml",
    "format": "./node_modules/.bin/prettier --write '**/*.ts' 'api-spec.yaml'",
    "lint": "./node_modules/.bin/eslint ./src/functions",
    "format:check": "./node_modules/.bin/prettier --check '**/*.ts'",
    "infra:format": "rain fmt --write template.yaml",
    "infra:format:check": "rain fmt --verify template.yaml",
    "invoke:JwksFunction": "sam local invoke JwksFunction --template .aws-sam/build/template.yaml --env-vars parameters.json",
    "localstack:down": "docker compose -f localstack/docker-compose.yml down",
    "localstack:up": "docker compose -f localstack/docker-compose.yml up --build -d --wait",
    "start:local": "npm run localstack:up && npm run invoke:JwksFunction && sam local start-api --template .aws-sam/build/template.yaml --warm-containers EAGER --env-vars parameters.json -p 6060",
    "test": "./node_modules/.bin/jest --silent"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.157",
    "@types/jest": "^30.0.0",
    "aws-sdk-client-mock": "^4.1.0",
    "aws-sdk-client-mock-jest": "^4.1.0",
    "eslint": "^9.39.0",
    "eslint-config-prettier": "^10.1.8",
    "jest": "^30.2.0",
    "jest-junit": "^16.0.0",
    "prettier": "^3.6.2",
    "ts-jest": "^29.4.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.46.2"
  },
  "dependencies": {
    "@aws-lambda-powertools/logger": "^2.28.1",
    "@aws-sdk/client-kms": "^3.922.0",
    "@aws-sdk/client-s3": "^3.922.0",
    "ecdsa-sig-formatter": "^1.0.11",
    "esbuild": "^0.25.12",
    "tsc": "^2.0.4"
  }
}
